version: '3'

services:
    db:
      image: mysql
      restart: always
      ports: 
        - 3306:3306
      environment:
        MYSQL_ROOT_PASSWORD: supersecret
        MYSQL_DATABASE: test
        MYSQL_USER: user
        MYSQL_PASSWORD: secret
